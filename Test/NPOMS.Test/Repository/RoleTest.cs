using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace NPOMS.Tests.Repository
{
	[TestClass]
    public class RoleTest
    {
        /*private readonly IRoleRepository _roleRepository;
        
        public RoleTest()
        {
            _roleRepository = new RoleRepository(ContextConfig.GetContext());
        }

        [TestMethod]
        public async Task Roles_Exists()
        {
            var roles = await _roleRepository.GetRolesAsync(false);
            Assert.IsTrue(roles.Count() > 0);
        }*/

    }
}
