using Microsoft.VisualStudio.TestTools.UnitTesting;
using NPOMS.Repository.Interfaces.Core;
using System.Threading.Tasks;

namespace NPOMS.Tests.Services
{
	/*[TestClass]
    public class DepartmentTest
    {
        private readonly IDepartmentRepository _departmentRepository;
        private readonly IDepartmentService _departmentService;

        public DepartmentTest()
        {
            _departmentRepository = new DepartmentRepository(ContextConfig.GetContext());
            _departmentService = new DepartmentService(MapperConfig.GetMapper(), null, _departmentRepository);
        }


        [TestMethod]
        public async Task GetDepartments()
        {
            var departments = await _departmentService.GetAll();
            Assert.IsTrue(departments.Count > 0);
        }
    }*/
}
